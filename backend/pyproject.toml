[project]
name = "flaechenschmiede-backend"
version = "0.0.0"
description = "Backend der parametrischen RC-Flugmodell-Konstruktionshilfe"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
